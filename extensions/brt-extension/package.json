{
  "name": "brt-extension",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node scripts/check-extension.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm run check && npm test"
  }
}
